#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here

# note: 
# this setup file is necessary if, for example, your CLI app require some initial setup so you run ./bin/setup on ther terminal and it will run the setup file and runs "bundle install"
